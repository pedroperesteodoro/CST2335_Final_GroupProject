name: final_project
description: "CST2335 group project — veterinary clinic app (main menu + modules)."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  # Floor = SQLite abstraction (see course SQLite.txt / https://pinchbv.github.io/floor/)
  floor: ^1.5.0
  sqflite: ^2.4.2
  encrypted_shared_preferences: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  floor_generator: ^1.5.0
  build_runner: ^2.4.13

flutter:
  uses-material-design: true
